# Copy to .env. Never commit .env.

# Claude, used to read the thesis and write one-line "why it fits" notes.
ANTHROPIC_API_KEY=

# Exa, the main search adapter (step 2).
EXA_API_KEY=

# Optional. Lifts GitHub search limits (step 4).
GITHUB_TOKEN=
